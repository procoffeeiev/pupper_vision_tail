# Python deps for the vision-reactive pupper.
# ROS 2, rclpy, vision_msgs, and the Hailo runtime are installed system-wide
# on the robot; pip is only for the extras used by this project's novel code.

PyYAML>=6.0
adafruit-circuitpython-servokit>=1.3
adafruit-blinka>=8.0
